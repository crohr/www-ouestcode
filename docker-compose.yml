version: "3.2"

networks:
  web:
    attachable: true

services:
  web:
    image: "ouestcode-web:20230328184031"
    build: .
    restart: unless-stopped
    ports:
      - "30000:80"
    networks:
      - web
